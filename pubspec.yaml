name: frhooks
description: Use react hooks in flutter!
version: 2.0.0
homepage: https://github.com/huangbinjie/frhooks

environment:
  sdk: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
